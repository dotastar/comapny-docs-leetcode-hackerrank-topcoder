I
Given an array of integers. We have to find the max element of the array, which is at multiple places in the array and return any one of the indices randomly.
According to the description, the max is guaranteed to be in multiple places.

II
what if we have to find the max element that appears multiple times in the array, and return the indices randomly?